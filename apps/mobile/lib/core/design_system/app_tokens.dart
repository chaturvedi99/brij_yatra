import 'package:flutter/material.dart';

/// Default divine palette; themes can override via remote `config_json.ui`.
abstract final class BrijTokens {
  static const peacock = Color(0xFF2E5077);
  static const softGold = Color(0xFFC9A227);
  static const vrindavanPink = Color(0xFFE8B4CB);
  static const sandalwood = Color(0xFFF5F0E6);
  static const deepMaroon = Color(0xFF5C1A1B);

  static const radiusLg = 20.0;
  static const spacing = 16.0;
}
